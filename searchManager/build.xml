<?xml version="1.0"?>

<project name="My Project" default="help" basedir=".">
  <!-- Define the properties used by the build -->
  <property file="build.properties"/>
  <property name="project" value="."/>
	
  <target name="help">
    <echo>You can use the following targets:</echo>
    <echo> </echo>
    <echo>  help    : (default) Prints this message </echo>
    <echo>  all     : Cleans, compiles, and packages application</echo>
    <echo>  clean   : Deletes work directories</echo>
    <echo>  compile : Compiles servlets into class files</echo>
    <echo>  dist    : Packages artifacts into a deployable WAR</echo>
    <echo>  resource: Copy resources into classpath</echo>
    <echo></echo>
    <echo>For example, to clean, compile, and package all at once, run:</echo>
    <echo>prompt> ant all </echo>
  </target>

  <!-- Define the CLASSPATH -->
  <path id="compile.classpath">
    <fileset dir="${tomcat.home}/bin">
      <include name="*.jar"/>
    </fileset>
    <pathelement location="${tomcat.home}/lib"/>
    <fileset dir="${tomcat.home}/lib">
      <include name="*.jar"/>
    </fileset>
    <fileset dir="${spring.lib}">
      <include name="*.jar"/>
    </fileset>
  </path>

  <target name="all" depends="clean,compile,dist,resource" description="Clean work dirs, then compile and create a WAR"/>

  <target name="clean" description="Delete old work and dist directories">
    <delete dir="${work.home}"/>
    <delete dir="${dist.home}"/>
  </target>

  <target name="prepare" depends="clean" description="Create working dirs and copy static files to work dir">
    <mkdir  dir="${dist.home}"/>
    <mkdir  dir="${work.home}/WEB-INF/classes"/>
    <!-- Copy static HTML and JSP files to work dir -->
    <copy todir="${work.home}">
      <fileset dir="${web.home}"/>
    </copy>
  </target>

  <target name="compile" depends="prepare"
          description="Compile Java sources and copy to WEB-INF/classes dir">
    <javac srcdir="${src.home}"
          destdir="${work.home}/WEB-INF/classes">
        <classpath refid="compile.classpath"/>
    </javac>
    <copy todir="${work.home}/WEB-INF/classes">
      <fileset dir="${src.home}" excludes="**/*.java"/>
    </copy>
  </target>

  <target name="dist" depends="compile" description="Create WAR file for binary distribution">
    <jar jarfile="${dist.home}/${app.name}-${app.version}-${app.build}.war" basedir="${work.home}"/>
    <delete dir="${work.home}"/>
  </target>
	
  <target name="resource">
	<echo message="Copy resources in ${resource.home}"></echo>
	<mkdir dir="${resource.home}" />
  	
  	<echo message="${project}/src"></echo>
  	
    <!-- Copy resources needed into classpath -->
	<copy toDir="${resource.home}">
		<fileset dir="${project}/src" casesensitive="no">
			<patternset>
				<include name="relevancy.xml"/>
				<include name="solr.xml"/>
			</patternset>
		</fileset>
	</copy>
 </target>
</project> 